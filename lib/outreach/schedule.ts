export function nextScheduleOccurrence(now:Date,timezone:string,localTime:string,weekdays:number[]):Date { const [h,m]=localTime.split(":").map(Number); const f=new Intl.DateTimeFormat("en-US",{timeZone:timezone,weekday:"short",hour:"2-digit",minute:"2-digit",hour12:false}); for(let i=1;i<=8*24*60;i++){const d=new Date(now.getTime()+i*60000),p=Object.fromEntries(f.formatToParts(d).map(x=>[x.type,x.value])),day=({Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6,Sun:7} as Record<string,number>)[p.weekday];if(weekdays.includes(day)&&Number(p.hour)%24===h&&Number(p.minute)===m)return d} throw new Error("Could not calculate schedule occurrence.") }
